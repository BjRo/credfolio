# Security Rules

- Never commit secrets. Use environment variables.
- `.env` files are ignored; provide `ENVIRONMENT.md` or docs instead of committing `.env.example` if blocked.
- Validate and sanitize inputs; log minimally sensitive data.


